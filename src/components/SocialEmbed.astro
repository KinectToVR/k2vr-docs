---
import { SITE } from '~/constants.js';
const { title, description, image, color, canonicalURL } = Astro.props;
---

<!-- Generic  -->
<title>{title}</title>
<link rel="canonical" href={canonicalURL}/>
{description && <meta name="description" content={description}>}
{color && <meta name="theme-color" content={color}/>}

<!-- OpenGraph  -->
<meta property="og:url" content={`https://${SITE.domain}`}>
<meta property="og:type" content="website">
<meta property="og:title" content={title}>
{description && <meta property="og:description" content={description}>}
{image && <meta property="og:image" content={`https://${SITE.domain}/${image}`}>}

<!-- Twitter  -->
<meta property="twitter:domain" content={SITE.domain}>
<meta property="twitter:url" content={`https://${SITE.domain}`}>
<meta name="twitter:title" content={title}>
{description && <meta name="twitter:description" content={description}>}
{image && <meta name="twitter:image" content={`https://${SITE.domain}/${image}`}>}
{image && <meta name="twitter:card" content="summary_large_image">}